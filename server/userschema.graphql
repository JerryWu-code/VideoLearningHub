scalar GraphQLDate

type User {
	_id: ID!
	id: Int!
	email: String!
	password: String!   
}

input InputUser {
	email: String!
	password: String!   
}

type Mutation {
	addUser(user: InputUser!): User!
}