scalar GraphQLDate

type User {
	_id: ID!
	id: Int!
	email: String!
	fullName: String!   
}

input InputUser {
	id: Int!
	email: String!
	fullName: String!   
}

type Mutation {
	addUser(user: InputUser!): User!
}

type Query {
  _empty: String  # Placeholder field to make Query type valid
}